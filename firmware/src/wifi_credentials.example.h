#pragma once
// 本机 WiFi 凭据模板：复制本文件为 wifi_credentials.h 并填入真实 SSID / 密码。
// wifi_credentials.h 已在 .gitignore 中，不会提交到仓库。
// 未创建 wifi_credentials.h 时，固件回退到串口命令 WIFI SET <ssid> <pass>。
#define STA_SSID "your-wifi-ssid"
#define STA_PASS "your-wifi-password"
